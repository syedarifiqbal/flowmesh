# Load exchange/queue definitions on startup
management.load_definitions = /etc/rabbitmq/definitions.json

# Increase message size limit to 128MB (default is 128MB, explicit for clarity)
max_message_size = 134217728

# Heartbeat interval in seconds
heartbeat = 60
